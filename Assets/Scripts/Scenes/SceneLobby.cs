using Common.Scene;
using UnityEngine;

namespace Scenes
{
    public class SceneLobby : SceneBase
    {
        public override bool Init(JSONObject param)
        {
            return true;
        }
    }
}
